# Local development defaults.
# Copy this file to .env if you want to override settings locally.

SECRET_KEY=local-development-secret-key-only
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost
CSRF_TRUSTED_ORIGINS=
SQLITE_NAME=db.sqlite3

# Optional production database connection.
# Keep this blank for local SQLite.
DATABASE_URL=
