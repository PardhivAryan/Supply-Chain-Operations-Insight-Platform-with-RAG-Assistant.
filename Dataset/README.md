# Dataset Folder

Place the DataCo SMART Supply Chain ZIP file in this folder when running the project locally.

ZIP files are intentionally ignored by Git because they are large data artifacts. The application can also accept the ZIP from the **Data Pipeline** page after login.
