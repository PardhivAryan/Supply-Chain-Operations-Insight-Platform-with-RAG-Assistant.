# Extracted Data Folder

This folder is populated automatically by:

```bash
python manage.py extract_dataset
```

Extracted CSV files are ignored by Git because they are generated from the dataset ZIP and can be recreated locally.
